<%= form_for(@check_group) do |f| %>
  <% if @check_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_group.errors.count, "error") %> prohibited this check_group from being saved:</h2>

      <ul>
      <% @check_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :refDate %><br />
    <%= f.datetime_select :refDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
